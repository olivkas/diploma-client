import React from 'react';
import PropTypes from 'prop-types';

const JumperModal = ({ jumper: { _id, shift } }) => {
  return <div>{_id}</div>;
};

JumperModal.propTypes = {};

export default JumperModal;
